{
  "name": "{{ name }}",
  "version": "{{ version }}",
  "description": "{{ description }}",
  "license": "{{ license }}",
  "scripts": {
    "upgrade": "yarn-upgrade-all",
    "test": "node index.js"
  },
  "devDependencies": {
    "standard": "^10.0.3",
    "yarn-upgrade-all": "^0.3.0"
  }
}
